name: Lint

on:
  push:
    branches: [main]
  pull_request:

permissions:
  contents: read

jobs:
  check-skills:
    name: Check skill files
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - name: Run scripts/check-skills.sh
        run: bash scripts/check-skills.sh
