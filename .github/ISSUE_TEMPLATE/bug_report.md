---
name: Bug report
about: A skill misfires — referenced tool no longer exists, query syntax broke, fix kit produces invalid output
title: "[skill-name] short description"
labels: bug
---

**Skill**
`/skill-name` (link the file in `commands/` if you can).

**What happened**
The exact failure — error text, the phase it happened in, the wrong output.

**What you expected**

**Project stack you ran it on**
Framework + version, database (Supabase cloud / self-hosted / plain Postgres), hosting, which MCP servers were connected.

**Claude Code version**
`claude --version`

**Anything else**
Sanitize before pasting: no real credentials, hostnames, CUI/VAT numbers, customer data.
